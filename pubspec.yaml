name: playgrid_mobile
description: PlayGrid Club mobile app
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod:
    path: packages/flutter_riverpod
  go_router:
    path: packages/go_router
  supabase_flutter:
    path: packages/supabase_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
