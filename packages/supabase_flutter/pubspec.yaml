name: supabase_flutter
description: Local placeholder Supabase client for PlayGrid Club.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
